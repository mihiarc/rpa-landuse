# Langchain Landuse Environment Configuration
# Copy this file to config/.env (recommended) or .env and fill in your actual values

# =============================================================================
# OpenAI Configuration (Required for AI Agents)
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI Organization ID (if you're part of an organization)
# OPENAI_ORG_ID=your_org_id_here

# =============================================================================
# Database Configuration
# =============================================================================
# Path to the main landuse DuckDB database
LANDUSE_DB_PATH=data/processed/landuse_analytics.duckdb

# Root directory for data files (used by general SQL agent)
PROJECT_ROOT_DIR=./data

# =============================================================================
# Agent Configuration
# =============================================================================
# LLM Model to use (options: gpt-4-turbo-preview, gpt-4, gpt-3.5-turbo)
LLM_MODEL=gpt-4-turbo-preview

# Temperature for LLM responses (0.0-1.0, lower = more deterministic)
LLM_TEMPERATURE=0.1

# Maximum tokens for LLM responses
LLM_MAX_TOKENS=2000

# Maximum iterations for agent reasoning
AGENT_MAX_ITERATIONS=5

# =============================================================================
# Query Configuration
# =============================================================================
# Default limit for SQL query results (safety feature)
DEFAULT_QUERY_LIMIT=100

# Enable verbose logging for debugging
AGENT_VERBOSE=true

# =============================================================================
# UI Configuration
# =============================================================================
# DuckDB UI port (when using duckdb -ui command)
DUCKDB_UI_PORT=4213

# Rich console theme (auto, light, dark)
CONSOLE_THEME=auto

# =============================================================================
# Development Configuration
# =============================================================================
# Enable development mode features
DEV_MODE=false

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Cache directory for temporary files
CACHE_DIR=.cache

# =============================================================================
# Example Usage Commands
# =============================================================================
# After setting up your .env file, try these commands:
#
# 1. Test the landuse natural language agent:
#    uv run python scripts/agents/landuse_query_agent.py
#
# 2. Test the general SQL agent:
#    uv run python scripts/agents/sql_query_agent.py
#
# 3. Run sample queries:
#    uv run python scripts/agents/test_landuse_agent.py
#
# 4. Start DuckDB UI:
#    duckdb data/processed/landuse_analytics.duckdb -ui
#
# =============================================================================
# Getting Your OpenAI API Key
# =============================================================================
# 1. Go to https://platform.openai.com/
# 2. Sign up or log in to your account
# 3. Navigate to API Keys section
# 4. Create a new API key
# 5. Copy the key and paste it above
# 6. Make sure you have billing set up for API usage
#
# Note: Keep your API key secure and never commit it to version control!
# =============================================================================
